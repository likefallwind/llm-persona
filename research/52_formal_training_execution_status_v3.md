# 正式训练阶段执行记录

快照时间：2026-09-05 05:36 UTC。此文件是运行记录；实时完成状态以阶段summary、完整性核验和真实进程为准。

## 已完成

- 测量试验160条导师回答、480份完整有效评审；两份结构/完成失败的原记录与同协议重试均保留。结果见research/49。
- 训练和确认各32道原创新题，分别30和32个保守来源家族；128次两模型内容审核及25条非空说明的agent复核。无新增人工标注。
- `design_freeze.json`于2026-09-05 05:14:48 UTC生成，在正式导师调用之前冻结研究问题、三个主要事件、全部输入、来源分界、提示臂和主要预测器代码。research/50保留的是冻结前书写的协议原文，其“准备冻结”状态由实际冻结文件与此执行记录更新，不修改已冻结字节。
- 正式训练生成1280/1280，均首个HTTP尝试成功，完成于05:35:18 UTC；结束状态、提示/回答/请求体哈希、参数及请求/返回模型核验全部通过。
- 五个部署配置各256条；没有把GLM-5.3重新命名为5.2。未把返回别名当成不可变权重证明。
- 3840份训练评审输入已冻结；自动检查确认全部评审payload排除了生成模型、角色臂、训练/确认身份和教师system原文，学生对话只含user消息。原文风格仍可能泄露身份。
- 全仓115项测试通过；其后新增的3项确认统计测试通过，覆盖六项Holm校正、来源内增加重复不虚增精度、零来源方差不自动判显著。

## 正在运行

训练三评审第一轮，计划3840份。生成完成后，训练接续程序已经从等待生成切换至 `judge_pass_0`，真实进程句柄本轮仍活动。

- 外部接续执行句柄：14975；进程PID：779518。
- 原训练生成句柄99852已正常退出0，不要重启整批生成。
- 当前状态：`training/run/pipeline_state.json`。
- 各阶段日志：`training/run/pipeline_logs/`。
- 评审输出：`training/judge/v2_2/run/responses.jsonl`。

上述路径相对于 `artifacts/educational_personality_v2/prospective_formal/`。PID和工具句柄仅用于本地续接，可能随运行结束失效；不作为独立完成证明，也不进入论文。

接续程序会在评审完成后核验全部记录，最多做两轮结构性修复（复用所有有效结果，只补失败项，原记录保留）。完整后自动拟合冻结的主预测器与只使用训练回答的长度/结构竞争画像，保存系数、训练输入顺序、预测、调参结果及哈希到预测锁。没有确认回答参与拟合。

**接续程序不会自动启动确认阶段。** 确认生成需要有效预测锁，当前尚不存在，因此4040条确认阶段生成及12120份确认评审尚未启动。训练锁完成后仍须核对结果完整性、测量表现和预测锁，再按已授权协议启动确认；不需要重新向用户请求例行实验许可。

## 论文与目标状态

新英文引言和方法写入 `paper/educational_personality_v3/sections/`，相关一手文献和证据主线见research/51。旧英文/中文论文与失败实验保持原样。新稿尚缺确认结果、完整图表、实证摘要与结论、中文稿、最终复现/渲染/声明审计。

本轮使正式独立验证进入实际执行，但顶会论文目标尚未完成。需要最终核查研究贡献与证据强度，不能以调用数量、测试或协议文件代替科学结果。

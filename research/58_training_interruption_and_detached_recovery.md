# 训练编码中断与持久后台恢复

日期：2026-09-05。运行恢复记录；不改变行为定义、主要终点、采样参数、预测方法或来源划分。

## 已核实的中断

原训练、确认和内容复核接续句柄均返回终止码 143。确认和内容接续最后的心跳约在 09:24 UTC 前，图表接续随后因前置确认未成功完成而退出。训练编码记录继续写入至 09:26:18 UTC，之后停止。宿主进程检查未发现仍运行的相关脚本，训练编码写入锁和各接续锁均已释放。没有把陈旧的 `status=running` 文件当作存活证据，也没有仅凭观察超时重启任务。

此次记录能证明进程已终止，不能识别发出终止信号的主体或根本原因。已提交但未保存完成记录的上游工作无法重建，也不计作新的独立观测。

## 恢复前审计

原始首轮共保存 3,238 条记录。冻结编码检查器确认 3,231 条结构有效；剩余 609 个请求包括：

- 602 个没有保存的完成记录；
- 三个 GLM 预算耗尽且无可见输出的完成失败；
- 四个 JSON 语法错误。

后三类均按既有的完成／结构失败规则处理。没有按行为标签、正例率、模型差异或评审分歧挑选重试。原始文件永久保留在 `training/judge/v2_2/run/`；`training/measurement_diagnostics/pass0/` 保存完整审计，`training/run/interruption_recovery.json` 保存源文件哈希与恢复脚本哈希。

## 接续方式与修复预算

`scripts/resume_personality_training_v3.py` 是本次中断的恢复入口。它检查原写入锁已释放、训练生成完整、冻结文件未变、修复目录尚未启动，然后复用全部 3,231 条有效记录到 `run_repair1`，从第一轮修复开始运行。最多使用原先保留的两轮修复（1、2），不会重跑首轮并重置已失败请求的预算。对未保存的请求而言，这一安排也不会增加原定的最大修复次数。

每轮仍使用相同请求、同一评审部署、温度 0、16,384-token 预算及每轮两次 HTTP 尝试。官方 MiniMax 合计并发上限仍为 4，Gateway 合计仍为 8。原始请求的时间、完成 ID 和返回文本随有效记录一并保留，不把复用记录计作新调用。训练完整通过后，继续使用原来的预测锁定及确认入口。

恢复脚本不自动反复重启。如果修复目录已存在，它要求检查当前状态，避免再次重置修复预算。

## 持久执行与状态判断

恢复会话：`tmux -L persona-v3-20260905_093519`，会话名 `persona-v3-recovery`。四个窗口分别执行训练恢复、确认接续、内容复核接续和本地结果处理。先验证训练接续已持锁，才启动确认窗口；确认持锁后再启动两个后继窗口。

启动记录与各阶段包装脚本位于：

`artifacts/educational_personality_v2/runtime/recovery_20260905_093519/`

每个阶段有 `.started`、`.log`、`.exit`、`.ended`，成功退出时另写 `.finished`。API 凭据只从环境窄范围继承到专用 tmux 服务，不写入参数、包装脚本、日志或记录文件。

窗口存活只能证明正在执行或等待。训练是否完成仍以完整测量审计、预测锁及训练接续最终状态为准；确认、稳健性、内容敏感性及最终论文各有独立完成要求。本记录不声称实验或论文完成。

# 训练完整编码、预测锁与确认启动

2026-09-05。训练测量及预测锁已完成，独立确认正在生成；论文未完成。

## 完成证据

- 1,280 条导师训练回答，3,840 份有效评审，10,240 个完整三评审多数事件标签。
- 原 16,384-token 预算下保留 3,839 份有效评审；research/59 的单项修订补回一份 GLM 编码。首次尝试在 40.592 秒完成，实际用了 3,511 completion tokens，结束状态为 stop。不能据此把“增加预算”解释为已经识别的故障原因。
- 原冻结审计器分别验证两种真实预算，新增合并程序验证严格不重叠与完整覆盖；所有先前失败输出和终态保留。
- 16 项定向测试通过，包括合并结果与原多数／一致性算法的合成对照、预算冒充拒绝、重复替换拒绝与缺失编码拒绝。测试不充当实证结论。

## 时间顺序

以下均为 UTC：

| 事件 | 时间 |
|---|---|
| 单项预算修订固定 | 10:59:18 |
| 修订请求开始 | 10:59:33.879425 |
| 修订请求完成 | 11:00:17.590436 |
| 主要／风格预测锁 | 11:00:25.606558 |
| 八组评审敏感性预测锁 | 11:00:52.204075 |
| 五组模型删除预测锁 | 11:01:03.509705 |
| 首个已保存确认请求开始 | 11:01:03.704632 |

主要锁包含 32 个事件—基线模型的 40,960 个预测，以及 8 个风格模型的 10,240 个预测。训练为 32 个模板、30 个来源家族；确认主要面板是 32 个新来源的 1,280 个待评分回答，不是未见模型或未见学科迁移。

`prospective_formal/training_completion_receipt.json` 在确认已保存 102 条记录时核对以上时间、九份协议／预测锁及合并输入的全部哈希，无不匹配。该收据只覆盖当时保存的记录；原确认控制器仍须对完成后的全部请求再核验时间顺序。

## 运行与论文

旧 tmux 四个窗口均以退出码 1 留存。新窗口 `budget_training` 已退出 0；`budget_confirmation` 执行确认生成，`budget_content` 与 `budget_reporting` 等待确认成功。它们位于同一 socket `persona-v3-20260905_093519`、session `persona-v3-recovery`；新日志与起止标记位于 `artifacts/educational_personality_v2/runtime/budget_amendment_20260905_105918/`。原恢复运行没有被覆盖。

中英文方法已经披露单份预算偏离，重建为 11 页英文和 8 页中文未完成工作稿。英文第 5 页、中文第 4 页的新增段落经渲染检查；完整最终结果和投稿版式尚待独立确认、内容敏感性及科学结论完成。

确认仍计划生成 4,040 条回答、取得 12,120 份有效评审；随后完成冻结预测比较、稳健性、内容错误敏感性与图表，再据真实结果重写摘要、结果、讨论和结论。当前不预判模型人格命题是否成立。

## 锁定预测的数值重建

11:07:17 UTC，新增只读数值核验完成。`scripts/verify_personality_forecast_artifacts_v3.py` 读取保存的训练输入、来源划分、系数和训练风格画像，调用冻结设计矩阵构造器，但不调用优化器，也不读取确认回答。

235 个拟合模型与 281,600 个保存概率全部逐项重建，最大绝对误差为 2.220446049250313e-16。检查覆盖主要／风格预测、八组评审面板和五组模型删除面板，同时核对每个目标单元的来源、学科、状态、模型、重复与请求身份，以及保存的 CV 损失对应的正则选择。

结果存于 `prospective_formal/forecast_reconstruction_audit/`，含逐模型误差、梯度和输入哈希。这是导出与重读一致性检查，不是独立实现或重新交叉验证，不构成预测准确性证据。冻结优化器允许成功终止时梯度高于 1e-6；此次最大重建梯度为 0.0002073941，原数值完整报告，不追溯修改已经锁定的拟合。

复现：`PYTHONDONTWRITEBYTECODE=1 OPENBLAS_NUM_THREADS=1 .venv/bin/python scripts/verify_personality_forecast_artifacts_v3.py`。

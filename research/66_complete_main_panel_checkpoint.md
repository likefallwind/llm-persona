# Analyze the complete main panel while an auxiliary probe is unresolved

Recorded on 2026-09-07 before this checkpoint is computed. The original
confirmation controller remains active, waiting for one GLM code on a
missing-information opportunity probe. All 3,840 main-panel responses already
have three separately validated coders under the disclosed budget contracts.
This includes all 1,280 canonical prediction responses and all 2,560 responses
under additional instruction arms. The unresolved probe cannot alter any of
these retained valid labels.

The checkpoint re-audits both immutable source runs, verifies an exact complete
main-panel coder/event grid, and invokes the original frozen functions for the
six primary tests, default and repeat profiles, paired prompt effects, eight
locked coder-panel sensitivities and five locked generator deletions. It uses
all main-panel data; it selects no outcomes, changes no hypothesis, refits no
model, imputes no missing value, and writes no full-study completion marker.
This is a scheduling change, not a new analysis of an incomplete main sample.

Results are saved separately in `prospective_formal/complete_main_checkpoint/`.
The original full-study outputs remain under their original controller. Once
they exist, corresponding tables must match the checkpoint numerically. The
200 training-source resamples, content sensitivity, budget sensitivity,
auxiliary diagnostics, final scientific interpretation and both manuscripts
remain unfinished. A checkpoint does not establish paper readiness.

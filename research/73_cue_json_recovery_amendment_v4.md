# v4 单条编码的机械语法修复

2026-09-08。原三轮补跑已在 15,359 / 15,360 条严格有效编码处停止。唯一缺口为 `99db96883c0bb0d19f7f:glm-5.3`：前两轮报告 length/空可见内容，最后一轮 stop 返回的八个事件对象、二元值和证据数组完整，但最外层对象缺一个右花括号。原 JSON 解码错误位置恰好位于文本末尾。

本修订只允许对这一个已记录的失败做派生语法恢复：去掉外层空白后，追加恰好一个 ASCII `}`，然后调用原来冻结的严格解析器。解析器仍要求唯一键、完整八事件、严格二元值和存在的非空来源证据行。不能改动标签、行号、字符串或其他标点；不能用已有多数标签选择修复方案。输入和程序哈希另立修订锁，原解析器、冻结设计、provider 原始记录、失败记录均不覆盖。此项是看到输出后的技术修订，不是原设计已经允许的成功结果。

恢复的八个值以派生 event_codes 保存，与原 15,359 条有效编码严格不相交地合并。它不是新 API 回答，不伪造完成 ID、原始 response hash 或采样参数。新的 API 调用为零。

同时计算唯一缺失编码取 0 和取 1 时每个事件的多数结果上下界，报告主事件是否对全部可能的缺失票均不变。该检验揭示缺口的影响，不用于决定是否修复。两个次事件若仍有不确定性，保留并披露，不能把主事件稳定扩展为八事件均稳定。

完成后运行原来冻结的 v4 预测分析，不拟合新参数、不重生成教师回答。论文完成、创新性和自然对话验证仍另行判断。

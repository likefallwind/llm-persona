# 档案最后两份 HTTP 400 的有限诊断修订

2026-09-08，原三轮任务已于 12:52:31 UTC 失败退出，修订在新诊断请求前形成。pass2 有 10,750 / 10,752 份结构有效编码，两个缺失请求均为 deepseek-v4-pro 的 HTTP 400；原运行器没有保存提供方错误正文，不能据此判断是暂时请求错误还是不可重试限制。

缺失 ID 固定为 `97c495a4d4eb3b96b91b:deepseek-v4-pro`、`5fb91d284325462763d5:deepseek-v4-pro`。选择完全由结构覆盖与传输状态决定，未查看两条回答的事件标签或其他编码者是否一致。

仅对这两个 ID 各增加一次相同请求，共最多两次 HTTP 尝试。仍用原 Gateway、模型、消息、temperature=0、max_tokens=32768、timeout=660 和非流式协议；串行执行，并持有原 Gateway 进程锁，不提高并发。仅增加错误正文的本地保存，不改材料、编码定义、预测或分析。原 10,750 份结构有效输出全部复用。原冻结文件、三轮返回及失败退出标记保留。

该操作是超出原固定三轮额度的公开诊断修订，不能算作原协议内自动补跑，也不改写原计划。它沿用用户对这些选定基准对话、参考解和 AI 回答发送至既定模型服务并产生费用的授权。请求、错误正文和返回仅放在忽略目录，不公开凭证或完整请求。

若两份都结构有效，保留实际取得的新输出并运行原冻结分析，同时报告修订及缺失编码敏感性。若仍失败，停止这个有限诊断，不自行循环加次数；依据保存的服务错误确定下一步。不得将缺失当负例、按其余评审多数补写第三份编码，或修改措辞绕过提供方拒绝。
